// Search for
      }
		}
	}
	if(pkDesc)
		pkDesc->SetRelay("");

  return (iExtraLen);
}

// Add above
#ifdef ENABLE_LOG_PM
				LogManager::instance().WhisperLog(ch->GetName(), pinfo->szNameTo, buf);
#endif

// Like this
#ifdef ENABLE_LOG_PM
				LogManager::instance().WhisperLog(ch->GetName(), pinfo->szNameTo, buf);
#endif
      }
		}
	}
	if(pkDesc)
		pkDesc->SetRelay("");

  return (iExtraLen);
}